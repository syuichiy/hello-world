# CLAUDE.md

このファイルは、このリポジトリで Claude Code（claude.ai/code）が作業するときの案内です。

## このプロジェクトについて

`hello-world` というお試し用のリポジトリです。今のところ `README.md` しか入っていないので、これから中身を足していきます。

## 会話のスタイル

- 通常の会話・応答は **標準語** で行ってください。
- 記事をマークダウンとして書く場合は、`write-article` スキルを使ってください（記事本文は九州弁で書きます）。

## 開発の進め方

- 変更を加えたら、分かりやすいコミットメッセージを付けてコミットしてください。
- プッシュするときは `git push -u origin <ブランチ名>` を使います。
- ネットワークの都合で失敗したときは、少し待ってからもう一度試してください。

## 気をつけること

- プルリクエストは、頼まれたときだけ作成してください。勝手には作らないこと。
- コミットする前に、正しく動くか確認してください。
- 判断に迷うときは、勝手に進めずに先に確認してください。

## スキル

- `write-article`: タイトルを入力するだけで、そのタイトルに沿った記事をマークダウン形式（九州弁）で生成するスキルです。
