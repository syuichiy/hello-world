#!/bin/bash
# 他の端末（同じWi-Fi内のスマホ等）からもアクセスできるように起動します。
# ダブルクリックで実行してください。起動後、ターミナルに表示される
#   http://<このMacのIP>:8765
# を、他の端末のブラウザで開いてください（同じWi-Fiに接続していること）。
cd "$(dirname "$0")"
exec bash ./run.command --lan "$@"
